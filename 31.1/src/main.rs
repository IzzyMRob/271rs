fn main(mut byte:u8) -> u64 {
    //take in a byte and return the number of non-0 characters
    // 1: need to parse through chars, turn into string
    // 2: if is non-0 add to weight
    // return total weight
    
   weight_u8();
}

fn weight_u8(byte:u8_ -> u64 {
    let mut weight : u64 = 0;
    for

}
